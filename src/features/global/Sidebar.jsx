import { Box, IconButton, Typography, useTheme } from '@mui/material';
import { Link } from "react-router-dom";
import {tokens} from "../../theme";
import { PeopleOutlined, HomeOutlined,  } from '@mui/icons-material';

const Sidebar = () => {
    return (
        <h1>
            SIDEBAR FOR NOW
        </h1>
    )
}

export default Sidebar;